import React from 'react';

const Home = () => {
    return (
        <div className='text-center'>
            {/* this is {user && <span>{user.displayName}</span>} */}
        </div>
    );
};

export default Home;